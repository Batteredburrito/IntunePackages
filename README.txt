package this app as an IntuneWin file. Specify the install command as:

powershell.exe -executionpolicy bypass -command "& '.\install.ps1'"

Detection rule is as follows:

Registry HKEY_LOCAL_MACHINE\SOFTWARE\McAfee 

Key Does Not Exist